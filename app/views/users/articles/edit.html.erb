
  <div class="card card-success">
    <div class="card-header">
      <h3 class="card-title">article編集</h3>
    </div>
    <!-- /.card-header -->
    <!-- form start -->
    <%= form_with model: @article, url: users_article_path(@article) do |f| %>
      <%= render 'form', f: f %>

      <div class="card-footer">
        <%= f.submit '更新', class: 'btn btn-success' %>
      </div>
    <% end %>
  </div>
